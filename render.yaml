services:
  - type: web
    name: attendanceiq-api
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python setup_db.py && gunicorn app:create_app()"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0